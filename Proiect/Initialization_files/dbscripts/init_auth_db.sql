CREATE TABLE IF NOT EXISTS Users(
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Username VARCHAR ( 50 ) UNIQUE,
    Password_hash VARCHAR(256) UNIQUE,
    Token VARCHAR(256) UNIQUE,
    Sess_updated TIMESTAMP
);